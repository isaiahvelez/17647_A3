apiVersion: apps/v1
kind: Deployment
metadata:
  name: mobile-app-bff
  namespace: bookstore-ns
spec:
  replicas: 2
  selector:
    matchLabels:
      app: mobile-app-bff
  template:
    metadata:
      labels:
        app: mobile-app-bff
    spec:
      containers:
        - name: mobile-app-bff
          image: YOUR_ECR_URI/mobile-app-bff:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 80
          env:
            - name: URL_BASE_BACKEND_SERVICES
              value: http://backend-router
          livenessProbe:
            httpGet:
              path: /status
              port: 80
            initialDelaySeconds: 15
            periodSeconds: 15
            timeoutSeconds: 5
